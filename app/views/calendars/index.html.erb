  <%= form_with model: @plan, url: calendars_path, class: 'form' do |f| %>
    <%= f.label :日付を選択, class: 'label' %>
    <%= f.date_field :date, class: 'date-select' %>
    <%= f.label :予定を入力, class: 'label' %>
    <%= f.text_field :plan, class: 'text-input' %>
    <%= f.submit '保存' %>
  <% end %>
  <div class='calendar'>
    <% @week_days.each do |day| %>
      <div class='item'>
        <div class='date'>
          <% day[:month] %>/<% day[:date] %>
        </div>
        <ul class='content'>
        <% if day[:plans].length != 0 %>
          <% day[:plans].each do |plan| %>
            <li class='plan-list'>・<%= plan %></li>
          <% end %>
        <% end %>
        </ul>
      </div>
    <% end %>
  </div>

<%# form_with の外側にある div タグが不要な理由は、要素の構造やスタイルに特に影響を与えていないためです。
１構造上の簡潔さ: 外側の div タグは、form_with で生成されたフォーム要素を囲む以外に特別な役割がない場合、削除することでコードが簡潔になります。
２スタイルの影響なし: 外側の div にクラスやスタイルが指定されていない場合、これを削除しても見た目やレイアウトには影響がありません。
特に、外側の div が特定のCSSスタイルやスクリプトで利用されていない場合は、不要であると判断できます。 %>